# ASR (Automatic Speech Recognition)
faster-whisper>=1.0.0
# Note: safetensors support is built into newer faster-whisper versions

# Core dependencies
streamlit>=1.28.0
transformers>=4.30.0
safetensors>=0.4.0
pydub>=0.25.1
tqdm>=4.65.0
python-dotenv>=1.0.0

# Audio processing
sounddevice>=0.4.6
soundfile>=0.12.1
numpy>=1.24.0

# Text-to-Speech
gtts>=2.3.0
pyttsx3>=2.90

# F5-TTS-THAI (optional, for premium Thai TTS)
torch>=2.0.0
torchaudio>=2.0.0
# f5-tts-thai (install via: pip install git+https://github.com/VYNCX/F5-TTS-THAI.git)

# OpenRouter API client
openai>=1.0.0

# HTTP requests
requests>=2.31.0

# RAG (Retrieval-Augmented Generation) dependencies
chromadb>=0.4.0
sentence-transformers>=2.2.0
PyPDF2>=3.0.0
faiss-cpu>=1.7.0

# Multi-Agent Systems
# Option 1: CrewAI (original implementation)
crewai>=0.28.0
litellm>=1.0.0

# Option 2: LangGraph (new implementation - recommended)
langgraph>=0.0.30
langchain>=0.1.0
langchain-openai>=0.0.5
langchain-community>=0.0.20

# Google Sheets integration
gspread>=5.0.0
oauth2client>=4.1.0

# Additional utilities
pandas>=2.0.0
pytz>=2023.0
pydantic>=2.0.0

# TensorFlow compatibility for Transformers
tf-keras

# PyTorch will be installed separately via setup script to avoid conflicts

# FastAPI Backend Server dependencies
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6
aiofiles>=23.2.1

# faster-whisper for optimized Thai ASR
faster-whisper>=0.10.0

# Audio processing for server
ffmpeg-python>=0.2.0

soundfile>=0.12.1
scipy>=1.10.0

# Multi-agent LLM orchestration (OpenRouter via LiteLLM)
crewai==0.86.0
crewai[tools]

librosa 
soundfile
nemo_toolkit[asr]

elevenlabs