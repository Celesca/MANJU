# Docker requirements - optimized for container environment
streamlit>=1.28.0
transformers>=4.30.0
pydub>=0.25.1
tqdm>=4.65.0

# Audio processing
soundfile>=0.12.1
numpy>=1.24.0

# HTTP requests for Ollama
requests>=2.31.0

# Additional utilities
pandas>=2.0.0

# TensorFlow compatibility for Transformers
tf-keras

# Note: PyTorch will be installed in Dockerfile
# Note: pyttsx3 and sounddevice disabled in Docker
# Note: FFmpeg installed via apt in Dockerfile
